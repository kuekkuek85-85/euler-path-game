# REVIEW — 한붓 챌린지 v1 구현 검토

PRD v1.0 대비 구현 결과 점검. 코드 리뷰(코덱스)에 앞서 **직접 확인한 것 / 확인하지 못한 것 /
PRD에서 벗어난 것 / 알려진 위험**을 나눠 적었다.

## 1. 검증 현황

```
유닛테스트   445개 통과 (5개 파일)
타입 검사    통과 (tsc --noEmit, strict)
ESLint       경고 0
프로덕션 빌드 통과 — 초기 청크 227KB (gzip 73KB)
브라우저 스모크 Playwright/Chromium, 390×844 · 844×390 · 820×1180 · 1280×800
```

테스트가 덮는 범위:

| 파일 | 무엇을 고정하는가 |
|---|---|
| `graph.test.ts` | 차수, 홀수점, 연결성, 오일러 판정, Hierholzer, 힌트의 다리 회피 |
| `stages.test.ts` | 전 스테이지의 이미지 대조표(점·선·홀수점) + 데이터 무결성 + 모든 시작점에서 해 존재 + 점 사이 간격 + 생성기 |
| `scoring.test.ts` | PRD 3.4 산식과 별점 경계값, 하한 50점, 상한 2000점 |
| `geometry.test.ts` | 다중 간선 곡선의 정점 좌표가 실제 베지어와 일치 |
| `format.test.ts` | 학번 검증, 이름 마스킹, CSV 이스케이프 |

## 2. 수용 기준 대조 (PRD 9)

| # | 기준 | 상태 | 근거 |
|---|---|---|---|
| AC-01 | 학번·성명 입력 후 3탭 이내 첫 스테이지 시작 | ✅ | 입장(시작하기) → 스테이지 목록 → L1-01 카드 = 3탭. 브라우저 스모크로 확인 |
| AC-02 | 스마트폰 드래그로 S08 오조작 없이 클리어 | ⚠️ 부분 | 390×844 에뮬레이터에서 **S08(간선 8개)을 한 번도 떼지 않고 완주해 클리어**했다. 히트 반지름은 44 CSS px 기준이되 이웃 노드 간격의 48%를 넘지 않게 제한한다. **실제 손가락 테스트는 못 했다** — 아래 4.1. 3.7의 엄격 모드 때문에 이 기준의 난이도가 올라갔으므로 실기기 확인이 더 중요해졌다 |
| AC-03 | S06을 짝수점에서 시작하면 반드시 막히고 안내가 뜬다 | ✅ | 유닛테스트(`solve(S06,'B')===null`) + 브라우저에서 B 출발 시 1개 남기고 막힘 토스트 확인 |
| AC-04 | 모든 DRAW 스테이지가 `solve()`로 해 존재 자동 검증 | ✅ | `stages.test.ts`가 허용된 **모든 시작점**에 대해 검사. 개발 모드 기동 시에도 자가 검사 |
| AC-05 | 기기를 바꿔 같은 학번으로 접속하면 기존 기록이 보인다 | ⚠️ 부분 | 규칙·읽기·쓰기 경로는 실제 프로젝트에서 검증했다(4.2). **두 기기로 이어받는 것 자체는 아직 실측 못 했다** |
| AC-06 | 대시보드가 다른 학생 클리어를 5초 이내 반영 | ⚠️ 미검증 | `onSnapshot` 실시간 구독으로 구현. 학생 2명이 있어야 실측 가능 |
| AC-07 | 네트워크를 끊어도 게임이 진행되고 복구 시 저장 | ✅ 전반 / ⚠️ 후반 | **Firebase에 닿지 못하는 실제 상태에서 브라우저로 확인** — 입장 151ms, S01 클리어 정상, 대기 배지 표시, 크래시 없음(4.2.1). 복구 시 큐 전송은 4.2가 끝나야 확인 가능 |
| AC-08 | 교사가 대시보드를 끄면 학생 화면에서 메뉴가 사라진다 | ✅ (규칙 변경 전제) | `config.dashboardVisible`이 false면 목록의 대시보드 버튼이 사라지고 `/dashboard` 직접 진입도 리다이렉트. **단 PRD 6.4 규칙을 그대로 쓰면 교사가 이 값을 못 바꾼다** — 아래 3.1 |
| AC-09 | 이름 마스킹 ON일 때 대시보드 어디에도 전체 이름이 없다 | ✅ | 랭킹·최단시간·우리 반·실시간 피드·교사 목록 5곳 모두 `displayName()` 경유. 마스킹 유닛테스트 포함. **단 2026-09-01 결정으로 기본값이 OFF라, 켜지 않으면 전체 이름이 보인다** — 3.9 |
| AC-10 | 25명 동시 접속 시 무료 할당량 내 1차시 운영 | ✅ (계산) | 아래 2.1 |

### 2.1 AC-10 읽기 횟수 추정

| 동작 | 읽기 | 25명 기준 |
|---|---|---|
| 입장 시 본인 문서 1회 | 1 | 25 |
| 스테이지 선택 화면 | 0 (메모리 상태 재사용) | 0 |
| 플레이·결과 | 0 (스테이지는 번들, 점수는 쓰기만) | 0 |
| 대시보드 랭킹 진입 | 최초 20 + 변경분 | 25명이 각 2회 열면 ≈ 1,000 |
| ~~실시간 피드~~ | **제거됨 (3.11)** | 0 |

한 차시 합계는 넉넉히 잡아도 2,000~3,000회 수준으로 일일 무료 5만 회 안에 들어온다.
실시간 피드를 뺀 덕에 예전 추정의 절반 아래로 내려갔다.
핵심은 **리스너를 대시보드 화면에서만 켜는 것**이고, 각 탭 컴포넌트가 마운트/언마운트에
맞춰 구독·해제하므로 보고 있는 탭 하나만 읽기를 낸다.

> 주의: 120명이 동시에 대시보드를 계속 켜 두면 실시간 갱신마다 읽기가 누적된다.
> 수업 중에는 교사 화면(프레젠테이션 뷰)만 띄우고 학생 대시보드는 정리 단계에서만 여는 운영을 권한다.

## 3. PRD에서 의도적으로 벗어난 부분

### 3.1 `config/global` 쓰기 규칙 — **가장 중요한 검토 포인트**

PRD 6.4 초안은 `allow write: if false`(교사 설정은 콘솔에서만)다.
그런데 PRD 5.5는 교사 모드에서 대시보드 On/Off·이름 마스킹·스테이지 활성화를 토글하라 하고,
AC-08은 그 결과가 학생 화면에 반영되기를 요구한다. 이 둘은 함께 성립할 수 없다.

**택한 쪽**: 필드 이름·타입·개수를 엄격히 검증하는 조건부 쓰기를 허용했다.

```
allow write: if signedIn()
  && request.resource.data.keys().hasOnly([...5개 필드...])
  && ...각 필드 타입 검증...
```

**받아들인 위험**: 교사 모드 진입은 환경변수 PIN으로 막지만, 규칙 자체는 "로그인한 누구나"를
허용한다. 개발자 도구를 다루는 학생이 `config/global`을 직접 쓸 수 있다.
피해 범위는 대시보드 노출·마스킹·스테이지 잠금 세 가지이며 기록은 건드리지 못한다.

**더 단단한 대안**(`firestore.rules` 주석에 코드까지 넣어 두었다): `config/admins` 문서에
교사 기기의 익명 uid를 콘솔에서 한 번 넣고, 규칙이 그 목록을 확인하게 한다.
운영 정책에 따라 고르면 된다. **어느 쪽을 쓸지는 결정이 필요하다.**

### 3.2 `students` 삭제 규칙
같은 이유로 `allow delete: if signedIn()`으로 열었다. PRD 5.5의 "개별 기록 삭제"를
수업 중에 쓰려면 필요하다. 3.1의 대안을 적용하면 이것도 함께 잠글 수 있다.

### 3.3 `GlobalConfig`에 `oddViewUnlocked` 추가
PRD 3.3은 "홀수점 보기 토글은 3단계부터 기본 해금, 2단계까진 교사 설정으로 개방"이라 했고
운영표(PRD 10)의 "정리 1"에서 이 토글을 해금하라고 되어 있는데, PRD 6.1 스키마에는
그 스위치가 없었다. 필드를 하나 늘리고 기본값 `false`로 두어 하위 호환을 지켰다.

### 3.4 JUDGE 스테이지 점수
PRD 3.5는 재시도 규칙만 정하고 점수를 정하지 않았다. **틀린 적 없이 맞히면 300점(★3),
한 번이라도 틀리면 150점(★1)**으로 정했다. `plays.score ≤ 2000` 규칙 안에 들어간다.

### 3.5 힌트가 "가도 되는 곳"만 보여준다
PRD 3.3은 "다음에 갈 수 있는 점을 반짝임"이라고만 했다. 그대로 만들면 힌트를 따라갔는데
막히는 일이 생겨 학습 경험을 해친다. `hintEdges()`는 각 선택지를 가정한 뒤 남은 그래프에
오일러 경로가 남는지 확인해 살아 있는 선택지만 반짝인다(사실상 Fleury의 다리 규칙).
살아 있는 선택지가 없으면(이미 실패한 상태) 갈 수 있는 곳을 모두 보여준다.

### 3.6 이름 길이 검사를 넉넉하게 잡았다
rules의 `string.size()`가 문자 수인지 UTF-8 바이트 수인지 문서로 확인하지 못했다
(이 개발 환경에서 firebase.google.com이 이그레스 정책에 막혀 있다).
"홍길동"은 문자로 3, 바이트로 9다. 한쪽만 가정하면 **한글 이름이 통째로 거부될 수 있어**,
양쪽 해석 모두에서 통과하는 2~15 범위를 썼다. 정확한 "한글 2~5자" 검사는
클라이언트(`lib/format.ts`)가 수행하고 유닛테스트로 고정돼 있다.

### 3.7 붓을 떼면 실패 — "한 번에 그리기" (2026-09-01 작성자 결정)

선을 그리던 중 포인터를 떼면 실패로 판정하고 처음부터 다시 그리게 한다(`'broken'` 상태).
한붓그리기라는 이름 그대로의 규칙이다. **PRD 3.2·3.3과 정면으로 충돌하므로**
아래를 함께 들어냈다. 되돌리기 전에 반드시 알고 있어야 할 부분이다.

| 없어진 것 | 이유 |
|---|---|
| **탭/클릭 조작** (PRD 3.2) | 탭은 정의상 매번 손을 뗀다. 마우스도 버튼을 누른 채 끌어야 한다 |
| **되돌리기 버튼·단축키 Z** (PRD 3.3) | 버튼이 캔버스 밖이라 누르려면 손을 떼야 한다 — 구조적으로 쓸 수 없다 |

정서적 안전(PRD 7.4)을 위해 두 가지를 덧댔다.
- 문구를 나무라지 않게 썼다: "붓을 뗐어요 / 선 N개까지 잘 갔어요. 다시 해볼까요?"
- **아직 선을 하나도 긋지 않았다면**(시작점만 눌렀다 뗀 경우) 실패로 치지 않고
  조용히 시작 전으로 되돌린다. 잘못 누른 것까지 벌하지는 않는다.

브라우저 실측(390×844): 2개 긋고 뗌 → 실패 오버레이 / 실패 후 추가 입력 무시 /
다시 그리기로 초기화 / 시작점만 눌렀다 뗌은 실패 아님 / **떼지 않고 8개 완주 → 정상 클리어**.

되돌리면(`useGameEngine`의 `breakStroke`와 `usePointerDraw`의 `onStrokeEnd`를 떼면)
탭 조작과 되돌리기가 그대로 살아난다.

### 3.9 이름 마스킹 기본값을 OFF로 (2026-09-01 작성자 결정)

PRD 5.4는 "이름 마스킹 옵션(교사 설정, **기본 ON**)"이고 근거는 "학급 내 공개 랭킹의
심리적 부담을 줄인다"였다. 작성자 요청에 따라 **기본값을 OFF로 바꿔** 대시보드에
전체 이름이 그대로 나오게 했다.

마스킹 **기능은 그대로 남아 있다.** 교사 모드의 "이름 마스킹" 토글을 켜면 즉시
`김민수 → 김O수`로 돌아가고, `maskName()`과 그 유닛테스트도 그대로다.

운영상 알아 둘 점: 대시보드가 학생에게 공개된 상태(`dashboardVisible: true`)에서는
반 전체가 서로의 전체 이름과 순위를 보게 된다. 노출을 줄이고 싶어지면 방법이 두 가지다.
1. 교사 모드에서 이름 마스킹을 다시 켠다.
2. 학생 대시보드 공개를 끄고, 정리 단계에 프레젠테이션 뷰만 띄운다.

### 3.10 보조 기능을 감췄다 (2026-09-01 작성자 결정)

"아이들이 스스로 고민하게" — **힌트와 다시하기 버튼을 없애고, 홀수점 보기는 교사가
켰을 때만** 나타나게 했다. 하단 컨트롤 영역은 홀수점 보기가 열렸을 때만 존재한다.

- 복구 경로는 남아 있다: 붓을 뗐을 때 캔버스에 뜨는 **"다시 그리기"** 오버레이 버튼.
  이것까지 없애면 막힌 학생이 빠져나올 방법이 사라진다.
- 교사 설정 `oddViewUnlocked`의 의미가 "1·2단계 조기 해금"에서 **"홀수점 보기 버튼을
  보여줄지"** 로 바뀌었다. 기본 꺼짐이고, 수업 운영표의 "정리 1"에서 켜면 된다.
  3단계라고 자동으로 열리지 않는다.
- 점수 산식의 힌트(-50)·재시작(-20) 차감은 그대로 두었다. 힌트는 이제 발생하지 않고,
  재시작은 붓을 뗀 뒤 "다시 그리기"를 누를 때 계산된다. 즉 **한 번 뗄 때마다 -20점**이다.

### 3.11 도전 회로를 고정 미션 5개로 (2026-09-01 작성자 결정)

PRD 4.2의 B02는 "매번 다른 도형이 나오는 무한 모드"였다. 이를 **고정 미션 5개**로
바꾸고 "새 도형 받기" 버튼을 없앴다.

| 스테이지 | 이름 | 점 | 선 | 기준 시간 |
|---|---|---|---|---|
| B02 | 도전 회로 1 | 9 | 12 | 60초 |
| B03 | 도전 회로 2 | 7 | 13 | 65초 |
| B04 | 도전 회로 3 | 8 | 14 | 70초 |
| B05 | 도전 회로 4 | 10 | 16 | 80초 |
| B06 | 도전 회로 5 | 12 | 18 | 90초 |

도형은 `lib/generator.ts`로 뽑은 결과를 `stages.json`에 그대로 심었고,
다른 스테이지와 똑같이 유닛테스트의 연결성·홀수점·해 존재 검증을 받는다.
간선 수가 뒤로 갈수록 늘어나는 것도 테스트로 고정했다.
`generator.ts`는 이제 런타임이 아니라 **설계 시점 도구**로 남아 있다(파일 상단 주석 참고).

생성기가 원래 12~14개밖에 못 만들었다(둘레 N각형 + 삼각형 3개씩이라 첫 12 이상에서
멈췄다). 목표 간선 수를 인자로 받게 고쳐 16·18개까지 뽑을 수 있게 했고, 같은 간선 수라면
**점이 많은 배치를 고르도록** 해서 선이 덜 겹치게 했다(B05는 7점/16선 대신 10점/16선).

> **B06은 S12와 같은 18선이다.** 3.7의 한 번에 그리기 모드에서 가장 어려운 판이므로,
> 4.1의 실기기 확인이 여기서도 그대로 필요하다.

부수 효과가 하나 있는데 오히려 이득이다: 도형이 고정되면서 대시보드의 "최단 시간"
순위를 도전 회로에서도 서로 비교할 수 있게 됐다. 매번 도형이 달랐을 때는 무의미했다.

### 3.12 대시보드 실시간 피드 제거 (2026-09-01 작성자 결정)

PRD 5.4의 "탭 4 · 실시간 피드"를 뺐다. `plays` 컬렉션을 통째로 실시간 구독해서
누가 클리어할 때마다 읽기가 쌓이는, 네 탭 중 부하가 가장 큰 화면이었다.
`subscribeFeed`와 `FeedRow`도 함께 지웠다. 2.1의 읽기 추정이 절반 아래로 내려갔다.

### 3.13 두붓 그리기 신설 (2026-09-01 작성자 제안)

PRD에 없던 모드다. S11·B01에서 "홀수점 4개 → 최소 2붓"이라고 **판정만** 하고 끝나던 것을,
**실제로 두 획으로 그려 보게** 한다. `Stage.maxStrokes`(기본 1)로 표현했다.

동작: 선을 긋던 중 붓을 떼면 실패가 아니라 **다음 붓**으로 넘어가고, 이때 현재 점이
비워져 **남은 선이 있는 아무 점에서나** 다시 시작할 수 있다. 붓을 다 쓴 뒤 또 떼면 실패다.
이번 붓으로 아직 아무 선도 긋지 않았다면 붓을 쓴 것으로 치지 않는다.

| 스테이지 | 도형 | 간선 | 비고 |
|---|---|---|---|
| C01 | 사각형과 두 대각선 (K4) | 6 | 가장 작은 두붓 도형 |
| C02 | 육각형과 지름 둘 | 8 | 끊는 자리를 골라야 한다 |
| C03 | 쾨니히스베르크, 두 붓으로 | 7 | **S11과 같은 도형** — 판정한 것을 실제로 그린다 |
| C04 | 5개의 방, 두 붓으로 | 16 | **B01과 같은 도형** |

검증: `solveInStrokes()`를 새로 만들어 **실제 2획 해를 구성적으로** 확인한다.
홀수점 2k개 중 2k-2개를 가상의 선으로 짝지어 한붓 경로를 만든 뒤 그 자리에서 잘라내는
방식이다. 유닛테스트가 네 스테이지 모두에 대해 (a) 2획 해가 실제로 나오고 (b) 모든 간선을
정확히 한 번씩 덮고 (c) `solve()`로는 풀리지 않으며 (d) 홀수점이 정확히 4개임을 검사한다.
AC-04의 두붓 판이다.

브라우저 실측: 붓 표시 "1 / 2" → 첫 붓 후 "2 / 2"로 전환(실패 아님) → 떨어진 점에서
두 번째 붓 시작 → 클리어. 세 번째로 떼면 실패 판정.

### 3.14 도전 경로 신설 (2026-09-01 작성자 요청)

보너스 구간이 회로형(B02~B06)뿐이라 **"시작점을 골라야 한다"**는 2단계의 핵심 기능이
도전 구간에 없었다. 홀수점이 정확히 2개인 큰 도형 3개를 추가했다.

| 스테이지 | 점 | 간선 | 홀수점 |
|---|---|---|---|
| D01 도전 경로 1 | 12 | 13 | 2 |
| D02 도전 경로 2 | 11 | 15 | 2 |
| D03 도전 경로 3 | 10 | 17 | 2 |

점이 10~12개나 되어 홀수점 둘을 눈으로 찾는 것 자체가 과제가 된다.
`generatePathStage()`를 만들어 뽑았다 — 짝수점만 있는 회로를 (목표-1)개로 만든 뒤
아직 이어지지 않은 두 점을 선 하나로 이으면 그 둘만 홀수가 된다.
유닛테스트가 **두 홀수점에서만 풀리고 나머지 모든 점에서는 `solve()`가 null**임을 검사한다.

### 3.15 캔버스를 화면 폭 가득으로, 지나간 선은 얇게 (2026-09-02 작성자 요청)

**"파란 선이 너무 굵어 그리기 힘들다"** + **"맵을 조금 키워 세로에서도 수월하게"**.

| 항목 | 전 | 후 |
|---|---|---|
| 지나간 선 굵기 | 4 | **3** |
| 체크 마크 | 폭 3.6 / 굵기 1.1 | 폭 3.0 / 굵기 0.95 |
| 캔버스 폭 (PRD 7.1) | 화면 폭의 92% | **가득 채움** (좌우 여백 `px-2`, 카드 `p-1.5`) |

측정값 (S12, 노드 12개 기준):

| 기기 | 캔버스 | 노드 최소 간격 |
|---|---|---|
| 390px (아이폰) | 337 → **362px** | **88px** |
| 360px (작은 갤럭시) | — → **332px** | **80px** |

세로에서는 캔버스가 정사각이라 **화면 폭이 한계**다. 390px 기기에서 여백을 다 걷어낸
362px가 사실상 최대치이고, 세로 여백은 남지만 더 키우면 좌우로 넘친다.

지나간 선을 얇게 해도 PRD 7.4("색만으로 정보를 전달하지 않는다")는 지켜진다 —
안 지난 선 2.2 대 지나간 선 3.0으로 굵기 차이가 남아 있고, 색과 체크 마크가 함께 붙는다.

### 3.16 다시하기가 타이머를 되돌리지 않는다
PRD 3.4의 차감 항목에 `재시작 횟수 × 20`이 있으므로, 재시작으로 시간이 초기화되면
사실상 무한 재시도가 유리해진다. 소요 시간은 스테이지 진입(카운트다운 종료) 시점부터
계속 흐른다. **다른 해석이 필요하면 알려 달라.**

## 4. 확인하지 못한 것 (정직하게)

### 4.1 실제 터치 기기 QA — PRD 8.4 11단계
Playwright/Chromium 에뮬레이터의 합성 포인터로만 확인했다.
**아이폰 SE / 갤럭시 / 아이패드 실기기 테스트는 하지 않았다.** 특히 확인이 필요한 것:
- 손가락 굵기로 S12(노드 12개, 간선 18개)를 **한 번도 떼지 않고** 그을 수 있는지
  — 3.7의 엄격 모드에서 가장 위험한 지점이다. 여기서 학생들이 반복 실패하면
  3.7을 되돌리거나 교사 설정으로 전환하는 것을 고려해야 한다
- iOS Safari에서 캔버스 위 스크롤·핀치가 실제로 막히는지 (`touch-action: none` + `preventDefault`)
- 화면 회전 시 진행 중인 판이 유지되는지 (React 상태는 유지되지만 실기기 확인 필요)

### 4.2 Firebase 실연결 — 규칙까지 실검증 완료 (2026-09-01)

익명 로그인 활성화와 규칙 배포가 끝난 뒤, 실제 프로젝트에 REST로 붙어 규칙 분기를
하나씩 확인했다. **8개 분기 모두 설계대로 동작한다.**

| 검사 | 기대 | 결과 |
|---|---|---|
| 익명 로그인 (`accounts:signUp`) | idToken 발급 | ✅ |
| `students` 생성 — 이름 "홍길동" | 통과 | ✅ 200 |
| `students` 점수 정상 증가 (0 → 560) | 통과 | ✅ 200 |
| `students` 한 번에 5000점 점프 | 거부 | ✅ 403 |
| `students` 점수 감소 시도 | 거부 | ✅ 403 |
| `plays` 정상 기록 (timeMs 9120) | 통과 | ✅ 200 |
| `plays` timeMs 500 | 거부 | ✅ 403 |
| `plays` 수정 시도 (추가 전용) | 거부 | ✅ 403 |
| `config/global` 읽기 | 통과(문서 미생성 404) | ✅ |

**3.6의 미확인 항목이 해소됐다.** rules의 `string.size()`가 문자 수든 바이트 수든,
현재 범위(2~15)에서 한글 이름 "홍길동"이 실제로 통과함을 확인했다.

여전히 남은 것은 **두 기기·두 학생이 있어야만 볼 수 있는 것들**이다:
- AC-05: 기기를 바꿔 같은 학번으로 접속했을 때 기록이 이어지는지
- AC-06: 다른 학생의 클리어가 대시보드에 5초 이내로 반영되는지
- AC-07 후반: 오프라인에서 쌓인 큐가 복구 시 실제로 전송되는지
- 대시보드 4개 탭의 복합 색인 (콘솔이 링크를 띄워 주면 클릭)

> 검증 과정에서 만든 테스트 기록이 `plays`에 **1건** 남아 있다(학번 19901, 1-99반 홍길동,
> S01). `plays`는 규칙상 삭제가 막혀 있어 앱에서 지울 수 없다. 대시보드 실시간 피드에
> 한 줄 보이므로, 거슬리면 **Firebase 콘솔 → Firestore → plays**에서 직접 삭제하면 된다
> (콘솔 삭제는 규칙을 거치지 않는다). `students/19901`은 이미 삭제했다.

### 4.3 동시 접속 부하
25명·120명 동시 접속을 실측하지 않았다. 2.1은 계산에 근거한 추정이다.

### 4.4 성취기준 코드
PRD 1.2 각주대로 2학기 정보과 교수학습 및 평가 운영 계획과 대조가 필요하다. 코드에는 반영하지 않았다.

## 5. 알려진 한계 (PRD가 이미 인지한 것)

- **기록 조작**: 점수를 클라이언트에서 계산하므로 개발자 도구를 다루면 조작이 가능하다.
  규칙으로 1회 증가 폭(2000)과 `plays.score` 상한을 두어 억제할 뿐 완전 차단은 못 한다.
  `plays.path`를 서버에서 재검증하는 기능은 v2 과제다.
- **다중 간선 선택**: 두 점 사이에 선이 여러 개일 때 드래그로는 "아직 안 쓴 첫 번째 선"이
  자동 선택된다. 현재 DRAW 스테이지에는 다중 간선이 없어 문제가 되지 않지만,
  v2에서 다중 간선 DRAW 스테이지를 넣는다면 선택 UI가 필요하다.
- **아주 빠른 클리어의 플레이 로그**: 규칙의 `plays.timeMs > 1000`(PRD 6.4)은 조작을
  억제하지만, S01(간선 3개)처럼 1초 안에 끝날 수 있는 판의 **정상 기록도 거부한다.**
  **2026-09-01 결정: 이 조건을 그대로 유지한다(작성자 확인).** 조작 억제 효과를 남기고,
  대신 클라이언트가 이런 영구 거부 항목을 큐에서 버리고 총점 저장은 계속 진행한다.
  따라서 잃는 것은 해당 판의 대시보드 "최단 시간" 한 줄뿐이고 총점·별점은 정상 저장된다.
  (되돌리려면 규칙의 그 줄을 `timeMs > 0`으로 바꾸면 된다.)

## 6. 코드 리뷰에서 특히 봐 주었으면 하는 곳

1. **`src/lib/graph.ts`의 `solve()`** — 반복형 Hierholzer. `cursor` 맵으로 이미 쓴 간선을
   건너뛰어 전체 O(간선 수)를 유지하려 했다. 다중 간선·루프 처리가 맞는지.
2. **`src/lib/repository.ts`의 `lazySubscribe()`** — Firestore 모듈이 도착하기 전에
   구독이 해제되는 경쟁 조건을 `cancelled` 플래그로 처리했다. 누수가 없는지.
3. **`src/hooks/useGameEngine.ts`의 `undo()`** — `usedEdges`와 `nodeHistory`(ref) 두 축을
   함께 되감는다. 같은 두 점을 잇는 선이 여러 개여도 정확히 직전 상태로 돌아가는지.
4. **`src/hooks/usePointerDraw.ts`의 히트 판정** — 44 CSS px 요구와 노드 간격 사이의
   타협(`min(44px 환산, 최소 노드 간격 × 0.48)`)이 S12·B02에서도 충분한지.
5. **`firestore.rules`** — 3.1의 결정이 타당한지, 규칙에 구멍이 없는지.
6. **점수 산식** — `timeBonus`에 `Math.floor`를 쓴 지점, 상한 2000 클램프가
   보안 규칙과 어긋나지 않는지.

## 7. 다음 할 일 (배포 전 체크리스트)

- [x] Firebase 프로젝트 생성 (완료) / Firestore DB 생성 (완료)
- [x] Authentication → 익명 로그인 활성화 (완료, 2026-09-01)
- [x] 보안 규칙 배포 + 8개 분기 실검증 (완료, 4.2)
- [x] `config/global` 문서 초기값 생성 — 교사 모드가 자동 생성하도록 변경(수동 작업 불필요)
- [x] `plays.timeMs > 1000` 유지 결정 (2026-09-01)
- [ ] 3.1의 `config/global` 쓰기 권한 방향 결정 (조건부 쓰기 유지 vs `config/admins` 허용 목록) — **미결**
- [ ] Vercel 환경변수 등록 (`VITE_TEACHER_PIN` 포함) ← 진행 중
- [ ] 실기기 QA — 아이폰 SE / 갤럭시 / 아이패드 (4.1)
- [ ] `/teacher` → 연결 상태 점검이 모두 ✅ 인지 확인
- [ ] 학생 2명으로 AC-05(기기 교체)·AC-06(실시간 반영) 실측
- [ ] 비행기 모드로 AC-07(오프라인 → 복구 저장) 실측
- [ ] 성취기준 코드 확정 (4.4)

---

## 8. 2026-09-06 · 미션 전면 재구성 (작성자 요청)

작성자가 준 이미지 20장(`Level 1-1` ~ `Level 2-10`)으로 스테이지를 다시 짰다.
기존 24개(S01~S12 · B01~B06 · C01~C04 · D01~D03)는 전부 삭제했고,
Firestore의 학생 기록도 함께 정리했다(§8.4).

### 8.1 무엇이 바뀌었나

| 항목 | 이전 | 지금 |
|---|---|---|
| 스테이지 | 24개 | **20개** (1레벨 10 + 2레벨 10) |
| id | `S01` `B02` `C03` `D01` | `L1-01`~`L1-10`, `L2-01`~`L2-10` |
| `Stage.tier` | 홀수점 개수 (1=0개, 2=2개, 3=복합) | **난이도 레벨** |
| 종류 | DRAW · JUDGE · 두붓(`maxStrokes:2`) | **DRAW 한붓만** |
| 잠금 | 구역별 | 앞 스테이지 하나 → 다음 하나 (단순 사슬) |
| 선택 화면 | 본편 3구역 + 보너스 + 도전 경로 + 두붓 | 레벨별 구역 (9절에서 레벨 선택 화면으로 다시 바뀐다) |

도형 구성(모두 한붓 가능):

| 레벨 | 회로형(홀수점 0) | 경로형(홀수점 2) | 간선 수 범위 |
|---|---|---|---|
| 1레벨 | L1-01·02·05·06·08 (5개) | L1-03·04·07·09·10 (5개) | 3 ~ 12 |
| 2레벨 | L2-09 (1개) | 나머지 9개 | 6 ~ 17 |

### 8.2 검토해 주었으면 하는 지점 — `tier`의 의미 변경

`validateStage()`에서 **"tier 1이면 오일러 회로, tier 2면 오일러 경로"** 검사를 없앴다.
`tier`가 이제 난이도 레벨이라 홀수점 개수와 묶이지 않기 때문이다(1레벨 안에 회로형과
경로형이 섞인다). 이 검사는 손으로 옮긴 좌표의 오타를 잡아 주던 안전망이었으므로,
`stages.test.ts`에 도형별 대조표(`EXPECTED`)를 만들어 옮겼다.

**위험**: 대조표도 사람이 쓴 것이라, 이미지를 잘못 읽었다면 대조표와 데이터가 사이좋게
틀릴 수 있다. 그래서 렌더 결과를 원본 이미지와 눈으로 대조했고, 20개 전부를
브라우저에서 실제로 그려 클리어했다(§8.3). 그래도 코드 리뷰 시 **원본 이미지와
`stages.json`을 한 번 더 대조**해 주면 좋겠다.

### 8.3 검증

```
유닛테스트    162개 통과 (5개 파일)
타입 검사     통과 (tsc --noEmit, strict)
ESLint        경고 0
프로덕션 빌드  통과 — 초기 청크 232KB (gzip 74KB)
브라우저 재생  20/20 클리어 (390×844, 앱 자신의 solve() 경로를 실제 드래그로 재생)
```

유닛테스트가 새로 고정하는 것:
- 도형별 점·선·홀수점 개수 (이미지 대조표)
- 홀수점 2개인 도형은 그 둘에서만 풀리고 나머지 점에서는 반드시 막힌다
- 홀수점 0개인 도형은 모든 점에서 풀린다
- 좌표가 5~95 범위 안, **점 사이 거리 ≥ 12 viewBox 단위**(362px 캔버스에서 약 43px)
- 중복 간선·자기 간선 없음, `unlockedBy`가 바로 앞 스테이지를 가리킴
- 기준 시간이 간선 수의 2~6배 사이

### 8.4 DB 정리

작성자 요청으로 시범 수업 기록을 지웠다. 지우기 전 전체를 JSON으로 백업해 전달했다.

| 컬렉션 | 지우기 전 | 결과 |
|---|---|---|
| `students` | 17명 (실제 학생 15 + 테스트 2) | **삭제 완료** (REST, 17/17) |
| `plays` | 330건 (2026-09-01 ~ 09-03) | **앱에서 삭제 불가** — `firestore.rules`의 `allow update, delete: if false`. 콘솔에서 컬렉션 삭제 필요 |

`plays`를 남겨 둔 것은 규칙을 일시적으로 열지 않기로 한 판단이다. 삭제 권한을 열면
그 사이 누구나 기록을 지울 수 있다. 어차피 사라진 스테이지 ID를 가리키는 기록이라
대시보드에는 잡히지 않는다.

### 8.5 살려 둔 미사용 코드

`lib/generator.ts`, `pages/JudgeBoard.tsx`, `components/decor/*`, `Stage.answer`,
`Stage.maxStrokes`, `Stage.decor`는 지금 쓰는 스테이지가 없다. 3레벨에서 판별 미션과
두붓 그리기를 되살릴 예정이라 지우지 않았다. 관련 로직(`solveInStrokes`, `calcJudgeScore`,
`breakStroke`의 다음-붓 분기)도 그대로이며 유닛테스트도 살아 있다.
**"지금 안 쓰니 지우자"는 리뷰 의견이 나온다면 3레벨 계획과 함께 판단해 주기 바란다.**

### 8.6 3레벨 추가 (10 / 10 완료)

작성자가 준 `Level 3-1` ~ `3-10` 이미지 10장을 옮겼다. 이로써 30개 미션이 모두 채워졌다.

| id | 이름 | 점 | 선 | 홀수점 |
|---|---|---|---|---|
| L3-01 | 보석 별자리 | 9 | 15 | 2 |
| L3-02 | 왕좌 | 11 | 15 | 2 |
| L3-03 | 나비 사슬 | 11 | 14 | 0 (회로) |
| L3-04 | 삼단 삼각형 | 9 | 15 | 0 (회로) |
| L3-05 | 화살 집 | 6 | 10 | 2 |
| L3-06 | 겹친 화살 | 7 | 10 | 2 |
| L3-07 | 작은 의자 | 9 | 13 | 2 |
| L3-08 | 큰 천막 | 7 | 10 | 0 (회로) |
| L3-09 | 마름모 창 | 8 | 10 | 0 (회로) |
| L3-10 | 아홉 칸 | 9 | 16 | 2 |

**이미지를 읽으며 판단한 곳 — 리뷰에서 확인 바람**

1. **L3-01 오른쪽 세로선 없음.** 왼쪽에는 위·아래 점을 잇는 세로선이 있으나 오른쪽에는
   없는 것으로 읽었다. 오른쪽에도 있었다면 홀수점이 4개가 되어 한 붓으로는 못 그린다.
   "풀리는 문제여야 한다"를 근거로 이 해석을 택했다.
2. **L3-03 좌우 대칭.** 가운데 X가 양쪽 세로선의 위 끝·아래 끝을 잇고 그 교차점에 별이
   있는 구조로 읽었다. 좌우 완전 대칭이고 홀수점이 0개로 떨어진다. 원본이 가로로 긴
   이미지라 비율을 유지한 채(가로 6~94, 세로 32~68) 넣어 정사각 캔버스에서는 작게 보인다.

3레벨은 1·2레벨과 달리 간선 수가 단조 증가하지 않는다(15·15·14·15·10·10·13·10·10·16).
작성자가 준 이미지 순서를 그대로 지켰기 때문이다. 대신 3레벨 평균 간선 수(12.8)가
1레벨 평균(7.6)보다 크다는 것만 테스트로 고정했다.

`STAGES_BY_LEVEL[n]`이 비면 스테이지 선택 화면에서 그 레벨 구역이 통째로 빠진다.
4레벨을 추가할 때도 `stages.json`과 대조표만 채우면 되고 화면 코드는 손댈 게 없다.

### 8.7 4레벨 추가 (10 / 10)

`Level 4-1` ~ `4-10` 이미지 10장. `Stage.tier`를 `StageLevel`(1~6) 타입으로 넓히고,
`STAGES_BY_LEVEL`·`LEVELS`·스테이지 선택 화면을 레벨 개수에 의존하지 않게 고쳤다.
이제 레벨을 늘릴 때 손댈 곳은 `stages.json`, 대조표, `StageLevel` 세 군데뿐이다.

| id | 이름 | 점 | 선 | 홀수점 |
|---|---|---|---|---|
| L4-01 | 다윗의 별 | 12 | 18 | 0 (회로) |
| L4-02 | 부채 별 | 7 | 10 | 0 (회로) |
| L4-03 | 철교 | 8 | 13 | 2 |
| L4-04 | 마름모 집 | 7 | 11 | 2 |
| L4-05 | 별 하트 | 10 | 11 | 2 |
| L4-06 | 쌍둥이 방 | 8 | 14 | 2 |
| L4-07 | 큰 창문 | 8 | 12 | 0 (회로) |
| L4-08 | 강아지 | 13 | 18 | 2 |
| L4-09 | 마름모 밭 | 16 | 28 | 0 (회로) |
| L4-10 | 성채 | 16 | 25 | 2 |

**이미지를 읽으며 판단한 곳 — 리뷰에서 확인 바람**

- **L4-02**: 긴 대각선 두 개(좌상↔우하, 우상↔좌하)는 가운데 작은 별을 *지나지 않고*
  스칠 뿐이라고 읽었다. 작은 별은 위 두 점에서 내려온 V의 꼭짓점(차수 2)이다.
  대각선이 그 별에서 갈라진다고 읽으면 홀수점이 2개가 되어 역시 풀리긴 하지만,
  그림의 "V가 거기서 끝난다"는 모양과는 맞지 않는다.
  **2026-09-06 수정**: 간선은 맞았으나 좌표가 틀렸다. 대각선의 교차점이 x=50에서
  y=54.5인데 그 점을 y=54에 두는 바람에 교차점이 점(반지름 3.2) 안에 완전히 묻혀,
  V의 꼭짓점이 아니라 "네 갈래 교차점"으로 보였다. 원본에서는 별이 X 위에 얹혀 있고
  교차점이 그 아래로 보인다. 점을 y=50으로 올려(위 V 꼭짓점도 37→35) 교차점과
  4.5단위를 벌렸다. **간선·점 개수·홀수점은 그대로다 — 순수한 배치 수정.**
  교훈: 이미지를 옮길 때 *간선이 지나가는 자리에 점을 놓지 않았는지*도 봐야 한다.
- **L4-07**: 아래 가운데 점을 여섯 갈래가 모이는 허브로 읽었다(위 가운데 점은 차수 2).
  반대로 읽으면(위가 허브) 역시 풀리지만, 왼쪽·오른쪽 바깥 점에서 뻗는 선의
  기울기가 위쪽 허브 해석과 맞지 않는다.
- **L4-08**: 오른쪽 아래 두 별 사이의 가로선이 있다고 읽었다. 없으면 홀수점이 4개가
  되어 한 붓으로 그릴 수 없다.

`4레벨은 회로형 4 · 경로형 6`이고, 평균 간선 수는 16.0으로 3레벨(12.8)보다 크다.

### 8.8 5레벨 추가 (10 / 10)

`Level 5-1` ~ `5-10` 이미지 10장. 코드 변경 없이 `stages.json`과 대조표만 채웠다
(8.7에서 레벨 개수에 의존하지 않게 고쳐 둔 덕분이다).

| id | 이름 | 점 | 선 | 홀수점 |
|---|---|---|---|---|
| L5-01 | 피라미드 | 10 | 18 | 0 (회로) |
| L5-02 | 부챗살 | 9 | 13 | 0 (회로) |
| L5-03 | 지그재그 | 8 | 11 | 2 |
| L5-04 | 별 액자 | 8 | 14 | 0 (회로) |
| L5-05 | 겹상자 | 12 | 20 | 0 (회로) |
| L5-06 | 쌍둥이 성 | 12 | 21 | 2 |
| L5-07 | 세 사각형 | 12 | 15 | 2 |
| L5-08 | 큰 부채 | 8 | 14 | 2 |
| L5-09 | 긴 창문 | 11 | 20 | 2 |
| L5-10 | 긴 별자리 | 14 | 21 | 2 |

**이미지를 읽으며 판단한 곳 — 리뷰에서 확인 바람**

- **L5-04**(별 액자): 선이 가장 촘촘해 판독이 어려웠다. 액자 네 변 + 대각선 둘 +
  위·아래 꼭짓점에서 가운데 두 점으로 가는 네 선 + 네 모서리에서 반대쪽 가운데 점으로
  가는 네 선, 모두 14개로 읽었다. 홀수점 0개로 떨어진다.
- **L5-08**(큰 부채): 위 꼭짓점과 아래 꼭짓점이 각각 여섯 갈래 허브라고 읽었다.
  오른쪽 세로줄의 가운데 점이 위·아래 양쪽과 이어지는지가 갈림길이었는데,
  이어진다고 보아야 홀수점이 2개로 떨어진다.
- **L5-10**(긴 별자리): 몸통 오른쪽 위 점과 배 가운데 점 사이의 선을 있다고 읽었다.
  없으면 홀수점이 4개가 되어 한 붓으로 그릴 수 없다.

### 8.9 6레벨 추가 (10 / 10 완료)

`Level 6-1` ~ `6-10`. 이로써 여섯 레벨 60개가 모두 채워졌다.

| id | 이름 | 점 | 선 | 홀수점 |
|---|---|---|---|---|
| L6-01 | 큰 물고기 | 10 | 15 | 2 |
| L6-02 | 왕관 띠 | 14 | 15 | 2 |
| L6-03 | 두 성문 | 14 | 22 | 2 |
| L6-04 | 바람개비 | 11 | 16 | 0 (회로) |
| L6-05 | 얽힌 별 | 7 | 15 | 2 |
| L6-06 | 별 그물 | 13 | 24 | 0 (회로) |
| L6-07 | 성곽 | 11 | 20 | 2 |
| L6-08 | 큰 별 | 8 | 10 | 2 |
| L6-09 | 밤하늘 | 12 | 18 | 2 |
| L6-10 | 쌍둥이 별 | 18 | 20 | 0 (회로) |

**L6-03 — 확실하지 않은 판독. 원본과 대조 부탁드립니다.**
가운데 나비 모양에서 **오른쪽 두 점을 잇는 세로선**을 넣었다. 이 선이 없으면
홀수점이 4개(왼쪽 두 점 + 가운데 오른쪽 두 점)가 되어 한 붓으로 그릴 수 없다.
"풀리는 문제여야 한다"를 근거로 넣었지만, 그림에서 그 세로선이 잘 보이지 않는다.
원본에 정말 없다면 다른 선 하나를 잘못 읽은 것이므로 알려 주시면 다시 맞추겠다.

**테스트 변경**: 레벨별 평균 간선 수의 단조 증가 검사를 **다 채운 레벨(10개)끼리만**
비교하도록 바꿨다. 채우는 중인 레벨은 평균이 아직 확정되지 않아 무의미하다.
대신 "레벨은 10개까지만 채우고, 덜 찬 레벨 뒤에는 스테이지가 없어야 한다"를 추가해
잠금 사슬이 중간에서 끊기는 사고를 막는다.

### 8.10 6-6 ~ 6-10에서 판단한 것

- **L6-06**(별 그물): 네 모서리에서 안쪽 격자로 뻗는 대각선을 모서리당 2개로 읽었다.
  1개씩만 있으면 네 모서리가 전부 홀수점이 되어 한 붓으로 그릴 수 없다.
- **L6-09**(밤하늘): 가운데 위 두 점을 잇는 짧은 세로선을 넣었다. 없으면 홀수점이
  4개가 된다. L6-03과 같은 종류의 판단이다.
- **L6-10**(쌍둥이 별): **원본 비율을 지키지 못한 유일한 스테이지다.**
  원본은 가로 1996 × 세로 1157(1.7:1)인데 캔버스는 정사각이고, 여덟 갈래 별 두 개 +
  잇는 사각형 = 점 18개를 손가락 간격(12 viewBox 단위 ≈ 43px) 이상으로 벌리려면
  별 하나의 반지름이 17을 넘을 수 없다. 그래서 두 별이 원본보다 작고, 잇는 사각형이
  두 별 사이가 아니라 아래쪽으로 내려가 있다. 간선 구성({8/3} 별 두 개 + 사각형
  네 변, 모두 20개)과 홀수점 0개는 원본과 같다.
  더 크게 그리려면 캔버스를 가로로 늘리거나(다른 스테이지 레이아웃에 영향) 점 간격
  기준을 낮춰야 하는데(휴대폰에서 오조작), 둘 다 값이 비싸서 지금 배치를 택했다.

## 9. 2026-09-06 · 스테이지 선택 두 단계 (작성자 요청)

미션 60개를 한 화면에 늘어놓던 것을 **레벨 선택(`/stages`) → 미션 선택(`/stages/:level`)**
으로 나눴다. 390×844 기기에서 레벨 선택 화면 문서 높이가 844px로 **스크롤이 사라졌다.**

| 항목 | 내용 |
|---|---|
| 새 화면 | `LevelStages` (`pages/StageSelect.tsx`), `components/LevelCard.tsx` |
| 새 함수 | `nextStage(best)` — 아직 깨지 않은 첫 스테이지. 유닛테스트 4가지 경우 고정 |
| 진행 표시 | 레벨 카드의 진행 막대 + `n / 10`, "지금 여기" 배지, 미션의 "여기부터 도전!" |
| 되돌아가기 | 상단 `← 레벨 선택`, 하단 `← 레벨 선택으로`, 휴대폰 뒤로 가기 모두 동작 |
| 잘못된 경로 | `/stages/9`처럼 없는 레벨은 `/stages`로 돌려보낸다 |

### 9.1 검토해 주었으면 하는 곳
- **잠긴 레벨은 `<div>`로, 열린 레벨만 `<a>`로 낸다.** 링크를 눌러도 못 들어가는 대신
  아예 링크가 아니게 했다. 대신 주소창에 `/stages/5`를 직접 쳐 넣으면 화면은 열린다 —
  거기서도 미션 카드가 전부 잠겨 있어 플레이는 막히지만, 레벨 화면 자체를 막지는 않았다.
  (막으려면 레벨 잠금 판정을 `LevelStages`에도 넣어야 한다. 지금은 "구경은 되지만
  못 푼다"가 학생에게 덜 답답하다고 판단했다.)
- **`nextStage()`는 "안 깬 첫 스테이지"이지 "열린 스테이지"가 아니다.** 사슬 구조에서는
  둘이 같지만, 교사가 스테이지를 비활성화하면 어긋날 수 있다. 그때도 "이어서 하기"는
  잠긴 미션을 가리킬 수 있고, 눌러도 `Play`가 `/stages`로 돌려보낸다.
- 색만으로 정보를 주지 않도록 진행 막대 옆에 항상 `n / 10` 숫자를 함께 둔다 (PRD 7.4).

### 9.2 7레벨은 없다 (2026-09-06 작성자 확인)

`StageLevel`은 1~6이고 여섯 레벨이 10개씩 빠짐없이 차 있다. **60개가 전부다.**

- 스테이지 선택 화면의 "다음 레벨 준비 중" 안내는 실행될 수 없는 분기가 되어 지웠다.
- 테스트의 "다 채운 레벨끼리만 비교"·"덜 찬 레벨 뒤에는 스테이지가 없다"도
  채우는 중일 때를 위한 장치였으므로, **"여섯 레벨이 10개씩 빠짐없이 차 있다"**
  하나로 바꿨다. 데이터가 실수로 빠지면 바로 걸린다.
- 레벨을 더 붙이게 되면 `StageLevel`·`LEVELS`·`stages.json`·대조표를 함께 넓히면 된다.

## 10. 2026-09-06 · "그림이 데이터와 다르게 보이는" 오류와 그 가드

작성자가 원본 이미지와 앱 화면을 대조해 L4-02·L4-07을 짚어 주었다. 둘 다
**연결성·홀수점 검사로는 절대 잡히지 않는** 종류의 오류였다.

### 10.1 두 가지 사고 유형

**(가) 간선 교차점이 점 안에 묻힌다** — L4-02
긴 대각선 두 개의 교차점이 x=50에서 y=54.5인데 가운데 점을 y=54에 뒀다.
0.5 단위 차이라 교차점이 점(반지름 3.2) 한가운데 들어갔고, 데이터상 차수 2인
V의 꼭짓점이 화면에서는 "네 갈래가 만나는 점"으로 보였다.

**(나) 간선이 무관한 점을 관통한다** — L6-08
왼쪽 위에서 오른쪽 끝으로 가는 긴 선이 가운데 위 점을 0.4 거리로 지나갔다.
원본에서도 그 별이 선 위에 놓여 있으므로, 한 간선이 아니라 그 점에서 끊긴
두 간선이 맞다(A-R → A-C, C-R). L6-08은 선이 10개에서 11개가 됐다.

### 10.2 고친 것

| 스테이지 | 문제 | 조치 |
|---|---|---|
| L4-02 | 교차점이 점 안에 묻힘 → **실은 간선 구성이 달랐다** | 아래 10.4 참고 |
| L4-07 | **간선 구성이 원본과 달랐다** | 위 가운데 점 → 좌·우 바깥 점 (위 모서리에서 반대쪽으로 가로지르는 것이 아니었다). 홀수점 0개 → 2개 |
| L6-05 | 간선이 가운데 점을 3.9로 스침 | 가운데 점을 x=56 → 58로 |
| L6-08 | 간선이 점을 0.4로 관통 | 그 점에서 간선을 끊어 둘로. 좌표도 원본 비율에 맞게 다시 잡음 |

L4-07만 간선 구성이 바뀌었고 나머지 셋은 배치 수정이다.

### 10.3 다시 생기지 않도록 — 기하 가드 (유닛테스트 120개 추가)

스테이지 60개마다 두 가지를 검사한다.

1. **간선이 무관한 점을 관통하지 않는다** — 점에서 간선까지의 거리 > 점 반지름(3.2)
2. **간선 교차점이 점 안에 묻히지 않는다** — 교차점에서 어느 점까지의 거리 > 3.2

가드가 실제로 잡는지 확인했다: L4-02를 옛 좌표(y=54)로 되돌리면
"L4-02 — 간선이 무관한 점을 관통하지 않는다"가 즉시 실패한다.

**L5-03은 기하 검사로는 깨끗했지만, 간선 하나가 빠져 있었다** — 아래 10.5 참고.
기하 가드로는 "빠진 간선"을 잡을 수 없다는 것을 다시 확인한 사례다.

### 10.4 L4-02 — 간선 구성이 달랐다 (작성자 정정)

처음에는 "좌표만 어긋났다"고 보고 점 위치를 옮겼는데, 작성자가 정점 번호로
정확히 짚어 주어 **간선 구성 자체가 틀렸다**는 것이 드러났다.

정점 번호(위에서, 왼쪽부터): 1 왼쪽 위 · 2 오른쪽 위 · 3 가운데 위 ·
4 작은 점 · 5 가운데 아래 · 6 왼쪽 아래 · 7 오른쪽 아래

| | 아래 모서리로 가는 긴 대각선 |
|---|---|
| 잘못 읽은 것 | **1-7, 2-6** (위 모서리에서 반대쪽 아래로 가로지름) |
| 원본 | **3-6, 3-7** (가운데 위 점에서 양쪽 아래로) |

바로잡으니 홀수점이 0개 → 2개(1번·2번)가 되고, 교차 위치도 원본처럼
가운데 좌우로 옮겨졌다. 좌표는 원본 비율(37 / 54 / 77)로 되돌렸다 —
교차점이 점에 묻히던 문제는 간선을 고치자 저절로 사라졌다.

**교훈**: 기하 가드는 "그림이 데이터와 다르게 보이는" 것은 잡지만,
"데이터가 원본과 다른" 것은 잡지 못한다. 후자는 원본과의 대조밖에 방법이 없다.
L4-07도 같은 경우였다.

### 10.5 L5-03 — 오른쪽 세로선이 빠져 있었다 (작성자 정정)

정점 번호(위에서, 왼쪽부터): 1 왼쪽 위 · 2 오른쪽 위 · 3 가운데 위 ·
4 가운데 왼쪽 · 5 가운데 오른쪽 · 6 가운데 아래 · 7 왼쪽 아래 · 8 오른쪽 아래

**간선 2-5**(오른쪽 위 → 오른쪽 가운데)를 빠뜨렸다. 원본에서 오른쪽 세로선은
맨 위부터 맨 아래까지 이어지는데, 나는 가운데부터 아래까지만 있다고 읽었다.
그 탓에 오른쪽 위 점이 한 갈래만 가진 외톨이가 되어 있었다.

- 선 11개 → 12개
- 홀수점은 여전히 2개지만 자리가 바뀌었다: (오른쪽 위, 가운데 왼쪽) → (가운데 왼쪽, 가운데 오른쪽)

**세 번째 같은 종류의 오류다(L4-02·L4-07·L5-03).** 기하 가드는 "그림이 데이터와
다르게 보이는" 것만 잡는다. 빠진 간선·잘못 이은 간선은 원본과의 대조로만 잡힌다.
남은 위험 구간은 REVIEW.md 8.7~8.10에 "판독이 갈릴 수 있다"고 적어 둔 곳들이다.

### 10.6 L5-04 — 가운데 두 점의 연결이 통째로 달랐다 (작성자 정정)

작성자가 원본을 보고 간선 16개를 번호로 직접 불러 주었다.

정점 번호((y, x) 순): 1 맨 위 · 2 왼쪽 위 · 3 오른쪽 위 · 4 가운데 왼쪽 ·
5 가운데 오른쪽 · 6 왼쪽 아래 · 7 오른쪽 아래 · 8 맨 아래

원본: `1-4 1-5 2-3 2-4 2-5 2-6 3-4 3-5 3-7 4-6 4-7 4-8 5-6 5-7 5-8 6-7`

| | 차이 |
|---|---|
| 뺀 것 | **2-7, 3-6** (액자를 가로지르는 긴 대각선 두 개) |
| 넣은 것 | **2-4, 3-5, 4-6, 5-7** (액자 네 모서리와 가운데 두 점을 잇는 짧은 사선) |

가운데 두 점(4·5)이 위아래 네 모서리와 모두 이어지면서 차수가 4 → 6이 되었다.

- 선 14개 → 16개, 목표 시간 56초 → 64초
- 홀수점은 그대로 0개(회로) — 모든 점이 짝수 차수(2·4·4·6·6·4·4·2)

**네 번째 같은 종류의 오류다(L4-02·L4-07·L5-03·L5-04).** 판독이 갈릴 수 있다고
적어 둔 남은 곳들(8.7~8.10: L4-08 · L5-08 · L5-10 · L6-03 · L6-06 · L6-09)도
원본과 대조받아야 한다.

### 10.7 L5-06 — X가 오른쪽 칸이 아니라 가운데 칸에 있다 (작성자 정정)

정점 번호((y, x) 순): 1 왼쪽 지붕 · 2 오른쪽 지붕 · 3~6 위 가로줄(왼→오) ·
7~10 아래 가로줄(왼→오) · 11 왼쪽 아래 꼭짓점 · 12 오른쪽 아래 꼭짓점

| | 간선 |
|---|---|
| 뺀 것 | **5-10, 6-9** (오른쪽 칸 안의 X) |
| 넣은 것 | **4-9, 5-8** (가운데 칸 안의 X) |

X 하나가 오른쪽 칸에서 가운데 칸으로 옮겨졌다. 오른쪽 칸은 대각선 없이
ㄷ자로 열려 있다.

- 선 21개 그대로, 목표 시간 84초 그대로
- 홀수점은 2개 그대로지만 자리가 바뀌었다: (가운데 기둥 위·아래) → (오른쪽 기둥 위·아래)
- 클리어 메시지도 새 홀수점 위치에 맞춰 고쳤다

**다섯 번째 같은 종류의 정정이다(L4-02 · L4-07 · L5-03 · L5-04 · L5-06).**

### 10.8 L6-07 — 맨 아래로 내려가는 세로선이 빠져 있었다 (작성자 정정)

정점 번호((y, x) 순): 1 지붕 · 2~4 위 가로줄(왼→오) · 5~7 가운데 가로줄(왼→오) ·
8~10 아래 가로줄(왼→오) · 11 맨 아래 꼭짓점

**간선 6-11**(가운데 점 → 맨 아래 꼭짓점)을 빠뜨렸다. 원본에서 맨 아래 점은
두 갈래인데, 나는 왼쪽 한 갈래(9-11)만 읽었다.

- 선 20개 → 21개, 목표 시간 80초 → 84초
- 홀수점은 2개 그대로지만 자리가 바뀌었다: (5번, 11번) → (5번, 6번) —
  맨 아래 점이 1갈래 → 2갈래가 되고, 가운데 점이 6갈래 → 7갈래가 되었다
- 클리어 메시지도 새 홀수점 위치에 맞춰 고쳤다

**여섯 번째 같은 종류의 정정이다(L4-02 · L4-07 · L5-03 · L5-04 · L5-06 · L6-07).**

### 10.9 L6-08 — 한가운데 교차점이 사실은 별이었다 (작성자 정정 + 원본 재확인)

가장 컸던 오독. 원본 한가운데에서 긴 직선 네 개가 엇갈리는데, 나는 그 지점을
**"선이 겹치는 교차점"으로만 보고 점을 8개로 읽었다.** 원본에는 거기에도 별이
있고, 그게 이 도형의 허브다.

정점 번호((y, x) 순): 1 왼쪽 위 · 2 오른쪽 위 · 3 가운데 위 ·
4 맨 왼쪽 · **5 한가운데 허브** · 6 맨 오른쪽 · 7 가운데 아래 ·
8 왼쪽 아래 · 9 오른쪽 아래

원본: `1-5 1-8 2-3 2-5 3-4 3-5 3-6 4-5 4-7 5-6 5-7 5-8 5-9 6-7 7-9`

허브를 지나는 긴 직선 네 개 — 1-5-9(↘) · 2-5-8(↙) · 4-5-6(가로) · 3-5-7(세로) —
가 각각 두 간선으로 나뉘어 허브 차수가 **8**이 된다.

- 점 8개 → 9개, 선 11개 → 15개, 목표 시간 44초 → 60초
- 홀수점 2개 그대로지만 자리가 바뀌었다: (2번, 4번) → **(4번, 6번)** — 좌우 맨 끝 두 점, 각 3갈래

작성자가 처음 준 목록에는 `2-8`(오른쪽 위 → 왼쪽 아래)이 통짜로 들어 있었는데,
그 선이 허브를 지나므로 `2-5 + 5-8`로 나눠야 한다. 통짜로 두면 홀수점이
4·5·6·8번 **네 개**가 되어 한 붓에 그릴 수 없었다. 작성자도 "홀수점은 4·6번"이라고
확인해 주어 나눠 읽는 쪽이 맞았다.

**교훈 하나 더**: 기하 가드는 "간선이 무관한 *점*을 지나가는지"는 잡지만,
**원본에서 점인 자리를 내가 점으로 안 찍었으면** 잡을 방법이 없다. 큰 교차점이
보이면 원본에서 거기에 표식이 있는지부터 확인해야 한다.

**일곱 번째 정정이다(L4-02 · L4-07 · L5-03 · L5-04 · L5-06 · L6-07 · L6-08).**

### 10.10 L6-09 — 위아래 허브 둘을 잇는 세로선이 빠져 있었다 (원본 대조)

작성자가 원본 그림에 **번호를 직접 적어서** 보내 주어 점 12개를 하나도 헷갈리지 않고
대조할 수 있었다. 데이터의 점 이름도 원본 번호 그대로 `N1`~`N12` 로 바꿔서,
앞으로 대조할 때 번호가 어긋날 여지를 없앴다.

원본 간선 20개:
`1-3 1-4 / 2-3 2-8 / 3-4 3-8 / 4-5 4-6 4-7 4-9 / 5-9 / 6-9 / 7-8 7-9 / 8-10 8-11 / 9-10 9-12 / 10-11 10-12`

작성자가 처음 준 목록(19개)과 두 곳이 달랐다.

| | 목록 | 그림 |
|---|---|---|
| 6번 별로 들어오는 선 | 5-6 (왼쪽 끝에서 가로로) | **4-6** (위쪽 허브에서 비스듬히) |
| 4번↔9번 | 없음 | **4-9** (두 허브를 잇는 세로선) |

5번 별에는 4번·9번으로 가는 선 두 개뿐이고 가로선은 없다. 그리고 4번과 9번은
거의 같은 x에 있어서 둘을 잇는 세로선이 그림에 뚜렷하다.

이렇게 읽으면 홀수점이 **7번·8번** 둘만 남아 한 붓에 그려진다. 목록 그대로면
홀수점이 5·7·8·9번 네 개라 두 붓이 필요했다.

- 점 12개 그대로, 선 18개 → 20개, 목표 시간 72초 → 80초
- 홀수점 자리: (9번 부근 허브 하나) → **7번·8번**
- 위쪽 허브 4번과 아래쪽 허브 9번이 각각 6갈래가 된다

**여덟 번째 정정이다(L4-02 · L4-07 · L5-03 · L5-04 · L5-06 · L6-07 · L6-08 · L6-09).**
원본에 번호를 적어 주는 방식이 가장 확실했다.
