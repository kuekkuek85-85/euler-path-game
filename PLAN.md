# PLAN — 한붓 챌린지 구현 계획

PRD v1.0 (2026-09-01) → 구현 계획. PRD 8.4의 태스크 분할을 그대로 따르되,
각 단계의 산출물과 "무엇으로 끝났다고 판단하는가"를 못 박았다.

## 0. 설계 원칙

1. **수학이 먼저다.** 스테이지가 실제로 풀리는지를 UI보다 먼저 증명한다.
   `lib/graph.ts`와 그 유닛테스트가 통과하기 전에는 화면을 만들지 않는다 (AC-04).
2. **게임은 네트워크에 의존하지 않는다.** Firebase가 없거나 끊겨도 전 스테이지를
   플레이할 수 있어야 한다 (PRD 7.3 / AC-07). 따라서 그래프·점수·진행 상태는
   순수 함수와 로컬 상태로만 만들고, Firestore는 그 위에 얹는 저장 계층으로 둔다.
3. **교실 기기가 기준이다.** 스마트폰 세로 360~430px에서 손가락으로 정확히 되는지가
   합격선이다. Pointer Events 하나로 터치·마우스·펜을 처리한다.
4. **실패를 나무라지 않는다.** 인접하지 않은 점으로 끌면 오답이 아니라 무시다.
   막힘 안내 문구에 부정 표현을 쓰지 않는다 (PRD 7.4).

> 1~4절은 2026-09-01 최초 구현 시점의 기록이다.
> **미션 구성은 5절이 최신이다** — 스테이지 표·아이디가 그때와 다르다.

## 1. 단계별 계획

| # | 단계 | 산출물 | 완료 판정 |
|---|---|---|---|
| 1 | 프로젝트 셋업 | Vite + React + TS + Tailwind v4 + React Router, `src/data/stages.json` (12 + 보너스 2) | `npm run build` 통과, 스키마가 `types.ts`와 일치 |
| 2 | 그래프 코어 | `lib/graph.ts`, `lib/generator.ts` + 유닛테스트 | 모든 스테이지가 `validateStage()` 통과, 선언한 tier와 실제 오일러 판정 일치 (AC-04) |
| 3 | 캔버스 | `components/GameCanvas.tsx`, `lib/geometry.ts`, `hooks/usePointerDraw.ts` | 히트 영역 ≥ 44 CSS px, 다중 간선이 곡률로 구분됨 |
| 4 | 게임 엔진 | `hooks/useGameEngine.ts` | 진행·undo·막힘·클리어 판정. S06을 짝수점에서 시작하면 반드시 막힘 (AC-03) |
| 5 | 결과·점수 | `lib/scoring.ts`, `pages/Result.tsx` | PRD 3.4 산식과 별점 규칙이 테스트로 고정됨 |
| 6 | Firebase | `lib/firebase.ts`, `lib/repository.ts`, 오프라인 큐 | 익명 인증, students/plays 읽고 쓰기, 끊겨도 플레이 지속 (AC-07) |
| 7 | 스테이지 선택 | `pages/StageSelect.tsx`, `components/StageCard.tsx` | 잠금·별점·최고 기록 표시, 3탭 안에 첫 스테이지 시작 (AC-01) |
| 8 | 대시보드 | `components/Dashboard/DashboardTabs.tsx`, `pages/Dashboard.tsx` | 4개 탭, 화면 이탈 시 리스너 해제 |
| 9 | 판별 미션 | `pages/JudgeBoard.tsx`, `components/decor/*` | S11·B01에서 가능/불가능 + 최소 붓 + 해설 카드 |
| 10 | 교사 모드 | `pages/Teacher.tsx` | PIN 게이트, 설정 4종, 스테이지 활성화, CSV, 개별 삭제 |
| 11 | 반응형 QA | — | 세로/가로/태블릿/데스크톱 스크린샷 확인 |
| 12 | 배포 준비 | `vercel.json`, `firestore.rules`, `firestore.indexes.json`, README | 색인 차단, 규칙·인덱스 문서화 |

## 2. 아키텍처 결정과 근거

### 2.1 스테이지 데이터는 정적 JSON
PRD 4.3 그대로. `src/data/stages.json`을 번들에 포함해 읽기 비용을 0으로 만든다.
`data/stages.ts`가 타입을 입히고, 개발 모드 기동 시 `validateStage()`로 자가 검사한다.
배포 빌드는 유닛테스트가 막는다 — 데이터 오류로 못 푸는 스테이지가 나가는 사고 방지.

### 2.2 힌트는 "다음 한 수"가 아니라 "어디서 시작할까"
(2026-09-07 작성자 요청으로 3단계 힌트를 넣으며 다시 정한 것)

이 수업의 결론은 "홀수점에서 출발해야 한다" 하나다. 그래서 힌트도 **시작점**만
답한다. 다음 한 수를 알려 주면 학생이 생각을 멈추고 힌트만 따라가게 되는데,
시작점 힌트는 오히려 결론을 향해 밀어 준다 — 막혔을 때 "시작점이 틀렸구나"를
스스로 정리하게 된다.

`startHintCandidates(stage, level)`가 1→3단계로 후보를 좁힌다.

- **포함 관계를 지킨다.** 3단계 ⊆ 2단계 ⊆ 1단계. 출발 가능한 점을 앞에 둔 목록에서
  앞부터 잘라 내므로, 단계가 올라가도 답이 후보 밖으로 밀려나지 않는다.
- **어느 단계에나 진짜 시작점이 들어 있다.** 60개 스테이지 전부에 대해 유닛테스트가
  이를 검사한다 — 힌트를 사고도 헛다리를 짚는 일이 없어야 한다.
- **무작위를 쓰지 않는다.** 같은 스테이지·같은 단계면 언제나 같은 답이다.
  다시 그려도 힌트가 흔들리지 않고, 테스트로 고정할 수 있다.
- **회로형은 문구가 다르다.** 홀수점이 없으면 아무 데서나 시작해도 되므로,
  3단계의 두 점은 "이 둘만 된다"가 아니라 "골라 보라"는 예시다. 여기서 말을 잘못하면
  배우는 내용 자체가 틀어진다.

감점은 20 / 40 / 80 — 많이 알려주는 단계일수록 크게 깎는다. 누르는 순간이 아니라
**얼마가 깎이는지 보여주는 확인창에서 "받을게요"를 눌러야** 깎인다. 13살 손가락에
80점짜리 버튼을 확인 없이 맡기지 않는다.

진행 중의 "다음 한 수" 힌트(`hintEdges()`)는 코드에 남아 있다. 각 선택지를 가정해 본 뒤
남은 그래프에 오일러 경로가 남는지 확인해서(사실상 Fleury의 다리 규칙) 살아 있는
선택지만 고르는 함수인데, 지금 화면에서는 쓰지 않는다.

### 2.3 Firebase는 동적 import
Firestore + Auth SDK는 gzip 약 150KB다. 정적으로 import하면 첫 화면이 그만큼 늦어진다.
`lib/firebase.ts`가 SDK를 동적으로 불러오고, `repository.ts`의 구독 함수는 해제 함수를
즉시 돌려준 뒤 모듈이 도착하면 리스너를 연결한다(`lazySubscribe`).
결과: 초기 청크 227KB(gzip 73KB), 게임 화면은 Firebase를 기다리지 않는다.

### 2.4 오프라인 큐를 Firestore 지속성 위에 따로 둔다
Firestore도 오프라인 쓰기를 큐에 담지만, 그 상태를 UI에 보여줄 방법이 마땅치 않다.
PRD 7.3이 요구하는 "기록 저장 대기 중" 배지를 위해 `localStorage`에 자체 큐를 두고
전송 성공 시에만 항목을 지운다. 온라인 복귀 이벤트와 30초 타이머로 재시도한다.

### 2.5 붓을 떼면 실패 — "한 번에 그리기" 모드
2026-09-01 작성자 결정. 한붓그리기는 말 그대로 한 붓에 그려야 하므로, 선을 그리던 중
포인터를 떼면 실패로 판정하고 처음부터 다시 그리게 한다(`GameStatus`의 `'broken'`).

이 결정은 PRD와 세 군데서 충돌하며, 아래처럼 정리했다.

| PRD | 충돌 | 처리 |
|---|---|---|
| 3.2 탭/클릭 조작, 두 방식 혼용 | 탭은 정의상 매번 손을 뗀다 | **탭 조작 제거.** 마우스도 버튼을 누른 채 끌어야 한다 |
| 3.3 되돌리기(무제한) | 버튼이 캔버스 밖이라 누르려면 손을 떼야 한다 | **되돌리기 버튼·단축키 Z 제거.** 구조적으로 쓸 수 없다 |
| 7.4 정서적 안전 | 손이 미끄러져도 실패한다 | 문구를 나무라지 않게("붓을 뗐어요", "선 N개까지 잘 갔어요") 쓰고, 아직 선을 하나도 긋지 않았다면(시작점만 눌렀다 뗌) 실패로 치지 않는다 |

되돌리기가 빠지면서 하단 컨트롤은 힌트·다시하기·홀수점 세 개가 된다.
막힘 안내 문구도 "되돌리기를 눌러 볼까요?" → "다시 그려 볼까요?"로 바꿨다.

2026-09-06 추가: "붓을 뗐어요" 오버레이는 3초 뒤 스스로 걷힌다(`BROKEN_AUTO_RESET_MS`).
이 오버레이를 벗어나는 길은 판을 처음으로 되돌리는 것뿐이라, 사라진다는 것은 곧
"다시 그리기"를 대신 눌러 준다는 뜻이다. 손이 미끄러질 때마다 버튼을 찾게 하지 않는다.
남은 시간은 버튼 아래 줄어드는 막대로 보여 준다.

### 2.6 프로필은 첫 렌더부터 로컬 값으로 시작한다
`/play/S12`를 직접 열거나 새로고침하면, 프로필이 비동기로 채워지는 동안 한 프레임
"아무것도 못 깬 학생"으로 보여 잠금 판정에 걸린다. `useState` 초기화 함수에서
`localStorage`를 바로 읽어 이 깜빡임을 없앴다.

## 3. PRD에서 벗어난 부분 (의도적)

| 항목 | PRD | 구현 | 이유 |
|---|---|---|---|
| `config/global` 쓰기 규칙 | `allow write: if false` | 필드 형태를 검증하는 조건부 쓰기 | 그대로 두면 AC-08(교사가 대시보드를 끄면 학생 화면에서 사라진다)을 앱 안에서 만족할 수 없다. 자세한 내용과 더 단단한 대안은 `firestore.rules` 주석과 REVIEW.md 참고 |
| `students` 삭제 규칙 | `allow delete: if false` | 인증된 사용자 허용 | 교사 모드의 "개별 기록 삭제"(PRD 5.5)를 수업 중에 쓰려면 필요하다 |
| `GlobalConfig` 필드 | 3개 | `oddViewUnlocked` 추가 | PRD 3.3의 "2단계까진 교사 설정으로 개방"과 수업 운영표의 "정리 1 — 홀수점 보기 토글 해금"을 실제로 켤 수단이 없었다 |
| JUDGE 점수 | 명시 없음 | 무오답 300점(★3), 오답 있으면 150점(★1) | PRD 3.5는 재시도 규칙만 정하고 점수를 정하지 않았다. `plays.score ≤ 2000` 규칙 안에 들어간다 |
| 이름 마스킹 기본값 | 기본 ON (PRD 5.4) | **기본 OFF** | 2026-09-01 작성자 결정. 대시보드에 전체 이름을 그대로 보여준다. 기능 자체는 그대로여서 교사 모드에서 언제든 다시 켤 수 있다 |
| 붓을 떼면 실패 | 없음 (탭 조작 허용) | 실패 판정 | 2026-09-01 작성자 결정. 위 2.5 참고 |
| 보조 기능 노출 | 되돌리기·힌트·홀수점 토글 상시 | **힌트·다시하기 버튼 제거, 홀수점 보기는 교사가 켤 때만** | 2026-09-01 작성자 결정 — "아이들이 스스로 고민하게". 붓을 뗐을 때의 "다시 그리기"는 캔버스 오버레이에 남아 복구는 항상 가능하다 |
| 힌트 | 회당 50점 정액, 다음 한 수를 알려줌 | **시작점 힌트 3단계 · 20/40/80점** | 2026-09-07 작성자 요청. 단계가 올라갈수록 후보를 좁히고 감점은 커진다. 위 2.2 참고 |
| 대시보드 실시간 피드 | 탭 4개 (PRD 5.4) | **탭 3개 — 실시간 피드 제거** | 2026-09-01 작성자 결정. `plays`를 통째로 실시간 구독해 네 탭 중 읽기 부하가 가장 컸다 |
| 캔버스 폭 | 화면 폭의 92% (PRD 7.1) | **가득 채움** | 2026-09-02 작성자 요청. 390px 기기에서 337 → 362px. 세로는 정사각 캔버스라 화면 폭이 한계다 |

## 4. 검증 방법

- **유닛테스트** (`npm test`): 그래프 이론, 점수 산식, 스테이지 데이터 무결성, 좌표 변환, 포맷터.
- **브라우저 스모크**: Playwright로 실제 드래그를 흉내 내 20개 스테이지 전부 클리어,
  붓 뗌 판정, 막힘 안내, 대시보드, 가로/태블릿 레이아웃을 확인.
- **수용 기준**: REVIEW.md의 AC-01 ~ AC-10 대조표.

## 5. 2026-09-06 · 미션 전면 재구성

작성자가 준 이미지 20장(`Level 1-1` ~ `Level 2-10`)을 기준으로 스테이지를 다시 짰다.
기존 24개(S01~S12 · B01~B06 · C01~C04 · D01~D03)는 전부 삭제했다.

| 항목 | 이전 | 지금 |
|---|---|---|
| 개수 | 24개 (본편 12 + 보너스 13) | **레벨당 10개** (1~6레벨 60개 — 이게 전부다) |
| id | `S01` `B02` `C03` `D01` | `L<레벨>-<번호>` (`L1-01` ~ `L6-10`) |
| `tier`의 뜻 | 홀수점 개수 (1=0개, 2=2개) | **난이도 레벨** (1레벨 / 2레벨 / 3레벨) |
| 미션 종류 | DRAW · JUDGE · 두붓 | **DRAW 한붓만** — 판별·두붓은 3레벨로 미뤘다 |
| 잠금 | 구역별 | 앞 스테이지 하나를 깨면 다음이 열리는 단순 사슬 |
| 선택 화면 | 5개 구역 | 레벨별 구역. 데이터가 없는 레벨은 통째로 빠진다 |

### 5.1 `tier`의 의미를 바꾸면서 잃은 검사를 되찾기
예전 `validateStage()`는 "tier 1이면 홀수점 0개"를 강제해 데이터 오타를 잡아 줬다.
`tier`가 레벨이 되면서 이 검사는 성립하지 않는다(한 레벨 안에 회로형과 경로형이 섞인다).
대신 `stages.test.ts`에 **도형별 대조표**를 두고 점·선·홀수점 개수를 못 박았다.
손으로 옮긴 좌표라 오타 하나면 못 푸는 미션이 배포되므로, 이 표가 없으면 안 된다.

### 5.2 검증 방법
- 유닛테스트 445개: 대조표, 연결성, 모든 유효 시작점에서의 해 존재,
  좌표 범위, 점 사이 최소 간격(12 viewBox 단위 ≈ 43px), 중복 간선,
  레벨끼리 평균 간선 수가 단조 증가.
- 브라우저: 전 스테이지를 앱 자신의 `solve()`가 낸 경로대로 실제 드래그해 클리어.
  60/60 성공, 콘솔 오류 없음.
- 렌더 결과를 원본 이미지와 눈으로 대조.

### 5.3 남겨 둔 것
`lib/generator.ts`(도형 생성기)와 `JudgeBoard` · `decor/`(판별 미션 UI)는 지금
쓰이지 않지만 3레벨에서 다시 쓸 수 있어 남겨 두었다. 생성기 테스트는 삭제된
스테이지를 템플릿으로 쓰고 있었으므로, 데이터에 기대지 않는 최소 템플릿으로 바꿨다.

## 6. 2026-09-06 · 스테이지 선택을 두 단계로

미션이 60개가 되면서 한 화면에 다 늘어놓으니 세로 스크롤이 8~9화면 길이가 됐다.
작성자 요청으로 **레벨 선택 → 미션 선택** 두 단계로 나눴다.

| 화면 | 경로 | 내용 |
|---|---|---|
| 레벨 선택 | `/stages` | 레벨 카드 6개 (2열 × 3행) + "이어서 하기" 버튼 |
| 미션 선택 | `/stages/:level` | 그 레벨의 미션 10개 + `← 레벨 선택` |

390×844 기기에서 레벨 선택 화면의 문서 높이가 정확히 844px — **스크롤이 없다.**

### 6.1 "어디까지 했지"를 화면이 대신 기억한다
같은 학번·이름으로 들어오면 기록을 이어받으므로(`students/{학번}`), 다시 들어왔을 때
어디부터 할지 찾는 수고를 없앴다. `nextStage()`가 아직 깨지 않은 첫 스테이지를
찾아 세 군데에 쓴다.

- 레벨 선택 맨 위의 **이어서 하기** 버튼 — 한 번에 그 미션으로 간다
- 그 미션이 든 레벨 카드에 **"지금 여기"** 배지와 파란 테두리
- 미션 목록에서 그 카드에 **"여기부터 도전!"**과 파란 테두리

기록이 중간에 비어 있어도(사슬이 끊긴 기록) 빈 곳을 먼저 가리키므로 안전하다.
유닛테스트가 이 네 가지 경우를 고정한다.

### 6.2 게임 안의 "← 목록"도 레벨 목록으로
플레이·판별·결과 화면의 목록 링크는 `/stages`가 아니라 방금 하던 레벨의
`/stages/:level`로 간다. 미션을 깨고 나오면 형제 미션들이 바로 보인다.
